fetchers_lib = static_library('fetchers', ['FinvizFetcher.cpp'])

# Declare dependency for the library to link to
fetchers_dep = declare_dependency(link_with: fetchers_lib, include_directories: include_directories('.'))
